% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_browse.R
\name{datalib_browse}
\alias{datalib_browse}
\title{Browse the library tree programmatically}
\usage{
datalib_browse(country = NULL, path = NULL, root = NULL)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).
Ignored when \code{path} is given.}

\item{path}{An absolute path previously returned in the \code{path} column;
when given, its children are listed and \code{country} is ignored.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.
Ignored when \code{path} is given.}
}
\value{
A data.frame with one row per choice and columns:
\describe{
\item{name}{character; display name (directories uppercased, file
names verbatim).}
\item{kind}{character; one of \code{"country"}, \code{"survey"}, \code{"vintage"},
\code{"section"} (\code{Data}/\code{Doc}/\code{Programs}/\code{Original}/\code{Stata}/\code{R}/\code{Other}),
\code{"folder"}, or \code{"file"}.}
\item{path}{character; absolute path to pass back as \verb{path =}.}
}
Zero rows when the node is empty.
}
\description{
Programmatic library navigation (choices only, v0.1). Replaces Stata
\verb{_foldernav}'s stateful SMCL click-chain with an explicit node: every
call returns a data.frame of choices; pass a returned \code{path} back as
\verb{path =} to descend. No interactive menu in v0.1.
}
\details{
With no arguments, lists the country folders under the resolved root.
With \code{country}, lists that country's survey folders (country is trimmed
and uppercased once, then matched exactly against folder names). With
\code{path}, lists the sub-directories and files of that node, classifying
each directory by the contract-v1 folder grammar.
}
\examples{
\dontrun{
datalib_browse()                          # countries under the root
datalib_browse("MWI")                     # MWI survey folders
node <- datalib_browse("MWI")
datalib_browse(path = node$path[1])       # descend one level
}
}
