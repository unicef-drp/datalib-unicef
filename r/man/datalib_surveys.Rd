% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_surveys.R
\name{datalib_surveys}
\alias{datalib_surveys}
\title{Enumerate survey folders for a country}
\usage{
datalib_surveys(country, root = NULL, survey = NULL, year = NULL)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}

\item{survey}{Optional survey acronym filter (e.g. \code{"MICS6"}). Trimmed
and uppercased once; rows must match it exactly.}

\item{year}{Optional survey year filter (integer or string); rows must
match it exactly.}
}
\value{
A data.frame with one row per survey folder and columns:
\describe{
\item{country}{character; uppercased country code.}
\item{year}{integer; survey year from the folder name.}
\item{survey}{character; uppercased survey acronym.}
\item{folder}{character; on-disk folder name.}
}
When any row has a non-missing year, the attributes \code{latest_year}
(integer) and \code{latest_survey} (character) identify the numerically
latest year and its survey -- numeric maximum, never listing order.
Zero rows when nothing matches.
}
\description{
Lists the \verb{<CCC>_<YYYY>_<SSSS>} survey folders of one country, mirroring
Stata \verb{_svycheck} with real lists and counts (no sentinels). Inputs are
trimmed and uppercased once at the boundary; the country is matched
exactly against folder names (never substring). Folders that do not
follow the three-token grammar are skipped.
}
\examples{
\dontrun{
datalib_surveys("MWI")
datalib_surveys("MWI", survey = "MICS6")
s <- datalib_surveys("MWI")
attr(s, "latest_year")
}
}
