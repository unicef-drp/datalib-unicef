% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_explorer.R
\name{datalib_explorer}
\alias{datalib_explorer}
\title{Inspect one node of any folder tree}
\usage{
datalib_explorer(path = NULL, root = NULL, sizes = FALSE, max_items = 400L)
}
\arguments{
\item{path}{Node to inspect, relative to \code{root}, used \strong{exactly} as
given: no case folding, no separator guessing. \code{NULL} (default) inspects
the root itself. Forward and backward slashes are both accepted.}

\item{root}{Tree to walk. Defaults to \code{\link[=datalib_root]{datalib_root()}}, but unlike every
other function here it does \strong{not} require that root to be a library.}

\item{sizes}{Measure each file's size. \code{FALSE} by default: a size needs
an \code{file.info()} call per file, and on the network share this package
targets that costs about 1.4 seconds each. When \code{FALSE} the \code{bytes}
column is \code{NA} rather than 0, because 0 is a real size.}

\item{max_items}{Cap on how many folders and how many files are returned.
Default 400. \code{n_dirs} and \code{n_files} always report the \emph{true} totals, and
\code{truncated} says when the cap bit.}
}
\value{
A list with:
\describe{
\item{root, path, fullpath, parent}{character. \code{parent} is \code{"."} at
depth 1 and \code{""} at the root.}
\item{depth}{integer, 0 at the root.}
\item{dirs, files}{character vectors, original casing, capped at
\code{max_items}.}
\item{n_dirs, n_files}{integer, the true totals.}
\item{exts, n_exts}{distinct extensions among the files returned,
lowercased and dot-free; \code{"none"} for a file without one.}
\item{bytes, largest, largest_bytes}{\code{NA} unless \code{sizes = TRUE}.}
\item{is_empty, truncated, looks_grammar}{logical.}
\item{open_with}{a data.frame of \code{file}, \code{ext} and \code{kind}
(\code{describe}/\code{view}/\code{open}), the same dispatch Stata's hyperlinks
use.}
}
}
\description{
Reports the contents of a single directory, whether or not it belongs to a
datalib library. Every other navigation function in this package
reconstructs a folder's ancestry from its own \emph{name} --
\code{\link[=datalib_browse]{datalib_browse()}} rebuilds \verb{CCC/CCC_YYYY_SURVEY/} by counting
underscores -- which is correct inside the naming grammar and useless
outside it: a folder called \verb{raw datasets} says nothing about which survey
it belongs to. \code{datalib_explorer()} applies no library test and no name
parsing; the only precondition is that the directory exists.
}
\section{Difference from the Stata implementation}{

Stata's \code{datalib_explorer} prints a \emph{clickable} SMCL listing, and that
half does not port: there is no console hyperlink in R. What ports is the
data, which was always the substantive half -- the Stata version's own
documentation calls its \code{r()} surface "as much the point as the display".
Where a Stata user clicks a folder, an R user calls this function again
with a longer \code{path}, which the returned \code{dirs} supports directly. The
file-type dispatch behind Stata's hyperlinks is reported here as
\code{open_with}, so a caller can act on it without reimplementing it.
}

\examples{
\dontrun{
node <- datalib_explorer(root = "<staging-tree>")
node$n_dirs
# descend: what a Stata user does by clicking
datalib_explorer("Afghanistan", root = "<staging-tree>")
}
}
\seealso{
\code{\link[=datalib_index]{datalib_index()}} to walk a whole subtree into a data.frame
instead of one node into a list.
}
