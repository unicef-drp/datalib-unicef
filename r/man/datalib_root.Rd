% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_root.R
\name{datalib_root}
\alias{datalib_root}
\title{Resolve the library root}
\usage{
datalib_root(root = NULL, user = NULL, config = NULL, configdir = NULL)
}
\arguments{
\item{root}{Explicit root path (highest precedence). When \code{NULL}, empty,
or \code{NA}, the remaining precedence chain is consulted.}

\item{user}{User name whose config block to read at the config stage.
When \code{NULL}, the logged-in user.}

\item{config}{Path to a single config file; when given, pins that file and
disables the two-file fallback.}

\item{configdir}{Directory in which to search the two-file list, instead of
\verb{~/.config}. Ignored when \code{config} is given.}
}
\value{
A length-1 character path, expanded and normalized with forward
slashes, carrying \code{source_stage} and \code{source_file} attributes.

Errors with condition class \code{datalib_error_input} (Stata 198) when no
source in the chain yields a root.
}
\description{
Resolves the datalib library root in the contract-v1 precedence order
(\code{config/grammar.md} section 7):
}
\details{
\enumerate{
\item explicit \code{root} argument;
\item environment variable \code{DATALIB_ROOT};
\item R option \code{datalib.root} (set via \code{options(datalib.root = ...)});
\item the \verb{datalib:} key in the current user's block of the config files,
searched in order: \verb{~/.config/user_config.yml} (the file shared with
the CSO Toolkit), then \verb{~/.config/datalib_config.yml}. The first file
whose user block carries a non-empty \verb{datalib:} key wins (key-presence,
block-level -- keys are never merged across files);
\item otherwise, an error.
}

The returned path carries two attributes recording where it came from:
\code{source_stage} (one of \code{argument}, \code{env}, \code{option}, \code{config_generic},
\code{config_package}) and \code{source_file} (the config file, when the root came
from a file; \code{""} otherwise). The \code{source_stage} strings are identical in
the R, Python, and Stata implementations.

Test/isolation hooks: set \code{DATALIB_CONFIG} to consult exactly one file
(fallback disabled), or \code{DATALIB_CONFIG_DIR} to search the two-file list
in a directory other than \verb{~/.config} (fallback preserved).
}
\examples{
\dontrun{
datalib_root("C:/data/library")           # explicit argument wins
Sys.setenv(DATALIB_ROOT = "C:/data/library")
datalib_root()                            # from the environment
r <- datalib_root()
attr(r, "source_stage")                   # where it resolved from
}
}
