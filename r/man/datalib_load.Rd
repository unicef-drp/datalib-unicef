% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_load.R
\name{datalib_load}
\alias{datalib_load}
\title{Load (and merge) survey modules}
\usage{
datalib_load(
  country,
  year = NULL,
  survey = NULL,
  module = NULL,
  kind = NULL,
  collection = NULL,
  master_version = NULL,
  adaptation_version = NULL,
  filename = NULL,
  merge = TRUE,
  root = NULL,
  verbose = FALSE
)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{year}{Survey year (integer or string). When \code{NULL}, the
numerically latest year among the country's matching survey folders.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once; survey folders match on the exact suffix \verb{_<SURVEY>} (never
substring). When \code{NULL}, taken from the numerically latest survey-year
folder.}

\item{module}{Character vector of module suffixes (e.g. \code{c("hl", "hh")})
naming files \verb{<vintage-stem>_<module>.dta}. When \code{NULL}, all modules
of the collection registry. Ignored when \code{filename} is given.}

\item{kind}{\code{"master"} or \code{"adaptation"}. Defaults to \code{"adaptation"}.}

\item{collection}{Adaptation collection code (e.g. \code{"HLT"}, the default
when \code{kind = "adaptation"}). Trimmed and uppercased once; applies only
to adaptations.}

\item{master_version}{Master vintage, accepted as \code{1}, \code{"01"}, \code{"v01"},
or \code{"V01"}. When \code{NULL}, the numerically latest master on disk
(numeric maximum, never alphabetical or listing order).}

\item{adaptation_version}{Adaptation vintage, same spellings. When
\code{NULL}, the numerically latest adaptation of the requested collection
under the chosen master.}

\item{filename}{Exact file name under the vintage's \code{Data/Stata} folder.
When given, that single file is loaded and returned as-is (no registry
lookup, no merge, no added columns).}

\item{merge}{Logical; when \code{TRUE} (default) the loaded modules are merged
into one data.frame. When \code{FALSE}, a named list of per-module
data.frames is returned instead.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}

\item{verbose}{Logical; when \code{TRUE}, a \code{message()} is emitted for each
file loaded.}
}
\value{
With \code{merge = TRUE} (default), one merged data.frame
(haven-labelled columns preserved; key and \code{ctrycode}/\code{year}/\code{source}
columns first). With \code{merge = FALSE}, a named list of module
data.frames. With \code{filename}, a single data.frame read as-is. In all
cases the attributes \code{datalib_files} (character; file names loaded)
and \code{datalib_resolve} (list; the full \code{\link[=datalib_resolve]{datalib_resolve()}} result)
record provenance.

Errors with condition class \code{datalib_error_input} (Stata 198) on invalid
input, missing files, or failed key validation.
}
\description{
Loads one or more survey modules from a resolved vintage's \code{Data/Stata}
folder, mirroring Stata \verb{_dlw} under contract v1 (\code{config/grammar.md}):
}
\details{
\itemize{
\item the vintage is resolved via \code{\link[=datalib_resolve]{datalib_resolve()}} (uppercase-once
normalization, exact matching, numeric latest defaults);
\item the module list defaults to the collection registry's full module set;
\item loaders NEVER mutate values (no recodes, no drops). The single
documented normalization: a person module whose registry \code{linevar}
differs from \code{line_number} has that variable renamed to \code{line_number}
on load (schema normalization for merging);
\item \code{ctrycode} (character) and \code{year} (integer) columns are added when
absent;
\item merge: person modules chain \code{1:1} (outer) on the registry's
\code{keys_person}, then hh-level modules attach \code{m:1} (outer) on
\code{keys_hh} (\code{1:1} when only hh modules). Keys are validated per module
(unique, no missings, \code{isid}-equivalent); failure is a typed error
naming the module. Unmatched rows are kept (outer join); the merge
indicator is not retained.
}
}
\examples{
\dontrun{
# latest HLT adaptation, all registry modules, merged
df <- datalib_load("MWI", 2019, "MICS6")
# two modules, unmerged
lst <- datalib_load("MWI", 2019, "MICS6", module = c("hl", "hh"),
                    merge = FALSE)
# one raw file from a master vintage
raw <- datalib_load("MWI", 2019, "MICS6", kind = "master",
                    filename = "MWI_2019_MICS6_v01_M_hh.dta")
attr(df, "datalib_files")
}
}
