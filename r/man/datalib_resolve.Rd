% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_resolve.R
\name{datalib_resolve}
\alias{datalib_resolve}
\title{Resolve a survey vintage to folder names and paths}
\usage{
datalib_resolve(
  country,
  year = NULL,
  survey = NULL,
  kind = NULL,
  collection = NULL,
  master_version = NULL,
  adaptation_version = NULL,
  root = NULL
)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{year}{Survey year (integer or string). When \code{NULL}, the
numerically latest year among the country's matching survey folders.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once; survey folders match on the exact suffix \verb{_<SURVEY>} (never
substring). When \code{NULL}, taken from the numerically latest survey-year
folder.}

\item{kind}{\code{"master"} or \code{"adaptation"}. Defaults to \code{"adaptation"}.}

\item{collection}{Adaptation collection code (e.g. \code{"HLT"}, the default
when \code{kind = "adaptation"}). Trimmed and uppercased once; passing it
with \code{kind = "master"} is an error.}

\item{master_version}{Master vintage, accepted as \code{1}, \code{"01"}, \code{"v01"},
or \code{"V01"} (normalized to the \verb{v\%02d} display form). When \code{NULL}, the
numerically latest master on disk.}

\item{adaptation_version}{Adaptation vintage, same spellings. When
\code{NULL}, the numerically latest adaptation of the requested collection
under the chosen master.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}
}
\value{
A named list (folder NAMES, like Stata, plus absolute paths):
\describe{
\item{root}{character; resolved library root.}
\item{country}{character; uppercased country code.}
\item{year}{integer; resolved survey year.}
\item{survey}{character; uppercased survey acronym.}
\item{kind}{character; \code{"master"} or \code{"adaptation"}.}
\item{collection}{character; collection code (\code{NA} for masters).}
\item{master_version}{integer; resolved master vintage.}
\item{adaptation_version}{integer; resolved adaptation vintage
(\code{NA} for masters).}
\item{survey_folder}{character; \verb{<CCC>_<YYYY>_<SSSS>} folder name.}
\item{vintage_folder}{character; resolved vintage folder name.}
\item{file_stem}{character; prefix of module files
(\verb{<vintage_folder>} under contract v1).}
\item{path}{character; absolute path of the vintage folder.}
\item{data_stata, data_original, data_r, data_other}{character;
absolute paths of the \code{Data/Stata}, \code{Data/Original}, \code{Data/R}, and
\code{Data/Other} sections.}
\item{doc, programs}{character; absolute paths of \verb{Doc/} and
\verb{Programs/}.}
}

Errors with condition class \code{datalib_error_input} (Stata 198) on invalid
input or when the country, survey, or vintage folder is not found.
}
\description{
Contract-v1 path resolution (pure; touches no data), mirroring
\code{stata/d/datalib_resolve.ado}:
}
\details{
\itemize{
\item inputs are trimmed and uppercased ONCE at the boundary (\code{kind} is
lowercased);
\item country folders match exactly; survey folders match on the exact
suffix \verb{_<SURVEY>} -- never substring;
\item "latest" defaults for year, master vintage and adaptation vintage are
the NUMERIC maximum (\code{v10} > \code{v09}), never alphabetical or
directory-listing order;
\item the resolved vintage folder is verified to exist before returning.
}
}
\examples{
\dontrun{
# latest HLT adaptation of the latest master
datalib_resolve("MWI", 2019, "MICS6")
# explicit master vintage
datalib_resolve("mwi", 2019, "mics6", kind = "master",
                master_version = "v01")
# latest year for the country ("latest" is numeric, never alphabetical)
datalib_resolve("MWI", survey = "MICS6", collection = "HLT")
}
}
