% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_update.R
\name{datalib_update}
\alias{datalib_update}
\title{Check whether a newer datalib is available}
\usage{
datalib_update(netsource = NULL, quiet = FALSE)
}
\arguments{
\item{netsource}{Directory holding the published \code{VERSION} manifest. When
\code{NULL}, the option, then the environment variable, then \verb{Z:/_pkg/datalib}.}

\item{quiet}{When \code{TRUE}, return the result without printing the report.}
}
\value{
Invisibly, a named list with elements \code{running}, \code{source},
\code{source_version} and \code{status}. \code{status} is one of \code{"current"},
\code{"newer_available"}, \code{"source_behind"} or \code{"unknown"}. \code{running} is \code{NA}
when \code{datalib} is not installed as a package -- for instance when it has
been sourced from a clone, in which case there is no installed version to
report.
}
\description{
Reports three coordinates: the version of \code{datalib} you are running, the
version published at the net site, and which way they differ. It is the R
counterpart of Stata's \verb{datalib , update}, and reads the \strong{same} version
manifest, so one publish serves all three languages.
}
\details{
\strong{It does not install anything.} \code{install.packages()} over a namespace that
is already loaded is unsafe on Windows -- the package's files are locked by
the running session -- so this function prints the exact command to run
instead of running it. That is a deliberate difference from Stata, where
\verb{datalib , update install} can replace ado-files in place because Stata has no
package manager and no loaded-namespace problem.

Why this exists at all, given R has \code{install.packages()} and
\code{update.packages()}: this package is \strong{not on CRAN}, and the repository it
lives in is private, so there is no registry for the usual machinery to query.
The net site is the only place a version can be discovered.

The net site is resolved in this order: the \code{netsource} argument, then
\code{options(datalib.netsource=)}, then the \code{DATALIB_NETSOURCE} environment
variable, then \verb{Z:/_pkg/datalib}.
}
\examples{
\dontrun{
datalib_update()
datalib_update(netsource = "Z:/_pkg/datalib")
r <- datalib_update(quiet = TRUE)
r$status
}
}
