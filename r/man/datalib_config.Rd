% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_config.R
\name{datalib_config}
\alias{datalib_config}
\title{Read the per-user configuration block}
\usage{
datalib_config(user = NULL, config = NULL, configdir = NULL)
}
\arguments{
\item{user}{User name whose block to read. When \code{NULL}, the logged-in
user reported by \code{Sys.info()[["user"]]}.}

\item{config}{Path to a single configuration YAML file. When given, it is
the only file consulted and the two-file fallback is disabled.}

\item{configdir}{Directory in which to search the two-file list, instead of
\verb{~/.config}. Ignored when \code{config} is given.}
}
\value{
A named list with elements (missing keys default to \code{""}):
\describe{
\item{user}{character; the user name that was looked up.}
\item{config}{character; path of the file the block was read from.}
\item{githubFolder}{character; the user's GitHub folder.}
\item{teamsRoot}{character; the user's Teams root.}
\item{zDrive}{character; drive to map (defaults to \code{"Z:/"}).}
\item{zDriveUNC}{character; UNC path behind the mapped drive.}
\item{datalib}{character; optional library root (\verb{datalib:} key).}
\item{source_stage}{character; where the library root came from --
\code{"config_generic"}, \code{"config_package"}, or \code{"unset"} when no file
carried the key.}
\item{source_file}{character; the file the library root came from, or
\code{""} when unset.}
}
}
\description{
Reads the current user's block from the configuration file(s) and returns
it. Mirrors the Stata \code{getuserconfig} command; unlike Stata, R sets no
globals -- callers hold the returned list.
}
\details{
Configuration resolution is a \strong{two-file, key-presence search}
(\code{config/grammar.md} section 7). The user's block is looked up first in
\verb{~/.config/user_config.yml} (the file shared with the CSO Toolkit) and then
in \verb{~/.config/datalib_config.yml}. The full block comes from the first file
that has the user block; for the library root, the \strong{first file whose block
carries a non-empty \verb{datalib:} key wins} (block-level -- keys are never
merged across files, and an empty value counts as absent). Where the root
came from is reported in \code{source_stage} / \code{source_file}.

Passing \code{config} (or setting the \code{DATALIB_CONFIG} environment variable)
pins exactly one file and disables the fallback; \code{configdir} (or
\code{DATALIB_CONFIG_DIR}) searches the two-file list in another directory.

Errors follow the contract-v1 taxonomy (\code{config/grammar.md}):
condition class \code{datalib_error_config_missing} (Stata 601) when no
configuration file exists, and \code{datalib_error_user_missing} (Stata 459)
when a file exists but carries no block for the user.
}
\examples{
\dontrun{
datalib_config()                          # block of the logged-in user
datalib_config(user = "jdoe")
datalib_config(config = "C:/cfg/user_config.yml")   # pin one file
datalib_config(configdir = "C:/cfg")                # search elsewhere
}
}
