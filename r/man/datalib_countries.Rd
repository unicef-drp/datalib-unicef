% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_countries.R
\name{datalib_countries}
\alias{datalib_countries}
\title{Enumerate countries in the library}
\usage{
datalib_countries(root = NULL)
}
\arguments{
\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}
}
\value{
A data.frame with a single column:
\describe{
\item{country}{character; unique uppercased country folder names.}
}
Zero rows when the root has no country folders.
}
\description{
Lists the country folders directly under the library root, mirroring
Stata \verb{_ctrycheck}: one row per country folder, uppercased, unique, in
directory-listing order.
}
\examples{
\dontrun{
datalib_countries()
datalib_countries(root = "C:/data/library")
}
}
