% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_vintages.R
\name{datalib_vintages}
\alias{datalib_vintages}
\title{Enumerate vintage folders of one survey}
\usage{
datalib_vintages(country, year, survey, root = NULL)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{year}{Survey year, an integer (or integer-like string) such as
\code{2019}.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once; the survey folder \verb{<CCC>_<YYYY>_<SSSS>} must match exactly.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}
}
\value{
A data.frame with one row per vintage folder and columns:
\describe{
\item{folder}{character; on-disk vintage folder name.}
\item{kind}{character; \code{"master"} or \code{"adaptation"}.}
\item{master_version}{integer; master vintage number.}
\item{adaptation_version}{integer; adaptation vintage number
(\code{NA} for masters).}
\item{collection}{character; collection code (\code{NA} for masters).}
\item{is_latest}{logical; numerically latest within its group (see
Details).}
}
Zero rows when the survey has no grammar-conforming vintages.
}
\description{
Lists every vintage folder (masters and adaptations) of one survey,
mirroring Stata \verb{_vcheck} with integer vintage columns (no MMAA
encoding). Inputs are trimmed and uppercased once at the boundary and
matched exactly against folder names; folders that do not follow the
contract-v1 vintage grammar are skipped.
}
\details{
\code{is_latest} is the NUMERIC maximum (\code{v10} > \code{v09}, never alphabetical or
listing order): among masters for master rows, and per
\verb{(master_version, collection)} group for adaptation rows.
}
\examples{
\dontrun{
v <- datalib_vintages("MWI", 2019, "MICS6")
v[v$is_latest, ]
}
}
