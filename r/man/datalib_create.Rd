% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_create.R
\name{datalib_create}
\alias{datalib_create}
\title{Create (or preview) a vintage folder tree}
\usage{
datalib_create(
  country,
  year,
  survey,
  kind = NULL,
  collection = NULL,
  master_version = NULL,
  adaptation_version = NULL,
  root = NULL,
  create = FALSE
)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once.}

\item{year}{Survey year, an integer (or integer-like string) such as
\code{2019}.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once.}

\item{kind}{\code{"master"} or \code{"adaptation"}. When \code{NULL}, defaults to
\code{"adaptation"} when \code{collection} is given, \code{"master"} otherwise.}

\item{collection}{Adaptation collection code (e.g. \code{"HLT"}). Trimmed and
uppercased once. Required when \code{kind = "adaptation"}.}

\item{master_version}{Master vintage, accepted as \code{1}, \code{"01"}, \code{"v01"},
or \code{"V01"} (normalized to the \verb{v\%02d} display form). Defaults to
\code{"v01"} when omitted.}

\item{adaptation_version}{Adaptation vintage, same spellings as
\code{master_version}. Defaults to \code{"v01"} when omitted (adaptations only).}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}

\item{create}{Logical; when \code{TRUE} the folder tree is created on disk.
The default \code{FALSE} is a dry run that only reports paths.}
}
\value{
A named list:
\describe{
\item{root}{character; resolved library root.}
\item{survey_folder}{character; \verb{<CCC>_<YYYY>_<SSSS>} folder name.}
\item{vintage_folder}{character; vintage folder name per the
contract grammar.}
\item{paths}{named list of character paths: \code{path}, \code{data},
\code{data_original}, \code{data_stata}, \code{data_r}, \code{data_other}, \code{doc},
\code{programs}.}
\item{created}{logical; \code{TRUE} when this call created the tree.}
\item{existed}{logical; \code{TRUE} when the vintage folder already
existed before the call.}
}

Errors with condition class \code{datalib_error_input} (Stata 198) on invalid
input.
}
\description{
Vintage-tree creator, check-mode by default. Mirrors
\code{stata/d/datalib_create.ado}: without \code{create = TRUE} it only reports the
paths that WOULD be made (zero side effects); with \code{create = TRUE} it
builds \verb{<vintage>/\{Data/\{Original,Stata,R,Other\},Doc,Programs\}}.
}
\details{
Inputs are trimmed and uppercased once at the boundary (contract v1).
Unlike \code{\link[=datalib_resolve]{datalib_resolve()}}, vintages default to \code{v01} for creation (not
the latest on disk), and \code{kind} defaults to \code{"adaptation"} when a
\code{collection} is given, \code{"master"} otherwise.
}
\examples{
\dontrun{
# dry run: report the paths that would be created
datalib_create("MWI", 2019, "MICS6", collection = "HLT")
# actually create a master vintage tree
datalib_create("MWI", 2019, "MICS6", kind = "master", create = TRUE)
}
}
