% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_files.R
\name{datalib_files}
\alias{datalib_files}
\title{List the files of one vintage section}
\usage{
datalib_files(
  country,
  year = NULL,
  survey = NULL,
  section = c("data", "data-original", "data-r", "data-other", "doc", "programs"),
  kind = NULL,
  collection = NULL,
  master_version = NULL,
  adaptation_version = NULL,
  root = NULL
)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{year}{Survey year (integer or string). When \code{NULL}, the
numerically latest year among the country's matching survey folders.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once; survey folders match on the exact suffix \verb{_<SURVEY>} (never
substring). When \code{NULL}, taken from the numerically latest survey-year
folder.}

\item{section}{Which vintage section to list: \code{"data"} (default,
\code{Data/Stata}), \code{"data-original"} (\code{Data/Original}), \code{"data-r"}
(\code{Data/R}), \code{"data-other"} (\code{Data/Other}), \code{"doc"} (\code{Doc}), or
\code{"programs"} (\code{Programs}).}

\item{kind}{\code{"master"} or \code{"adaptation"}. Defaults to \code{"adaptation"}.}

\item{collection}{Adaptation collection code (e.g. \code{"HLT"}, the default
when \code{kind = "adaptation"}). Trimmed and uppercased once; applies only
to adaptations.}

\item{master_version}{Master vintage, accepted as \code{1}, \code{"01"}, \code{"v01"},
or \code{"V01"}. When \code{NULL}, the numerically latest master on disk
(numeric maximum, never alphabetical or listing order).}

\item{adaptation_version}{Adaptation vintage, same spellings. When
\code{NULL}, the numerically latest adaptation of the requested collection
under the chosen master.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}
}
\value{
A data.frame with one row per file and columns:
\describe{
\item{file}{character; file name as on disk.}
\item{module}{character; module suffix for grammar-conforming \code{.dta}
files in the data section, otherwise \code{NA}.}
\item{path}{character; absolute path to the file.}
\item{section}{character; the requested section.}
}
Zero rows when the section is empty or missing.
}
\description{
Lists the files of one section (\code{Data/Stata}, \code{Data/Original}, \code{Data/R},
\code{Data/Other}, \code{Doc}, or \code{Programs}) of a resolved vintage, replacing Stata
\verb{_foldernav}'s DATA/DOC/PROGRAMS branches with a plain data.frame: one row
per file. The vintage itself is resolved with \code{\link[=datalib_resolve]{datalib_resolve()}} under
contract-v1 semantics (uppercase-once normalization, exact matching,
numeric latest defaults). Section names are shared across the three
implementations.
}
\details{
For \code{section = "data"}, the \code{module} column carries the module suffix
parsed from \verb{<vintage-stem>_<module>.dta} (\code{NA} when the file does not
follow the grammar); \code{module} is \code{NA} outside the data section.
}
\examples{
\dontrun{
datalib_files("MWI", 2019, "MICS6")                      # data files
datalib_files("MWI", 2019, "MICS6", section = "doc")     # documentation
datalib_files("MWI", kind = "master", section = "programs")
}
}
