% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_catalog.R
\name{datalib_catalog}
\alias{datalib_catalog}
\title{Catalog every survey vintage in the library}
\usage{
datalib_catalog(root = NULL, country = NULL)
}
\arguments{
\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}

\item{country}{Optional country code filter (e.g. \code{"MWI"}). Trimmed and
uppercased once; matched exactly against country folder names. When
\code{NULL}, all countries are scanned.}
}
\value{
A data.frame with one row per survey vintage, sorted by country,
year, survey, kind, collection and version, with columns:
\describe{
\item{country}{character; uppercased country code.}
\item{year}{integer; survey year from the folder name.}
\item{survey}{character; uppercased survey acronym.}
\item{kind}{character; \code{"master"} or \code{"adaptation"}.}
\item{master_version}{integer; master vintage number.}
\item{adaptation_version}{integer; adaptation vintage number
(\code{NA} for masters).}
\item{collection}{character; collection code (\code{NA} for masters).}
\item{survey_folder}{character; on-disk survey folder name.}
\item{vintage_folder}{character; on-disk vintage folder name.}
}
Zero rows when nothing matches.
}
\description{
One-pass tree scan of the whole library: one row per survey vintage
(master or adaptation), mirroring Stata \code{datalib_catalog}. Folder names
are parsed with the contract-v1 \code{"_"}-split grammar
(\code{config/grammar.md}); folders that do not follow the grammar are
skipped. The optional \code{country} filter is trimmed and uppercased once
and matched exactly (never substring).
}
\examples{
\dontrun{
datalib_catalog()                      # the whole library
datalib_catalog(country = "MWI")       # one country
datalib_catalog(root = "C:/data/library")
}
}
