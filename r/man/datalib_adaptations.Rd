% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_adaptations.R
\name{datalib_adaptations}
\alias{datalib_adaptations}
\title{Enumerate adaptations of one survey}
\usage{
datalib_adaptations(country, year, survey, root = NULL)
}
\arguments{
\item{country}{Country code (e.g. \code{"MWI"}). Trimmed and uppercased once;
matched exactly against country folder names (never substring).}

\item{year}{Survey year, an integer (or integer-like string) such as
\code{2019}.}

\item{survey}{Survey acronym (e.g. \code{"MICS6"}). Trimmed and uppercased
once; the survey folder \verb{<CCC>_<YYYY>_<SSSS>} must match exactly.}

\item{root}{Library root path. When \code{NULL}, resolved by \code{\link[=datalib_root]{datalib_root()}}
in contract precedence order: explicit argument, then the
\code{DATALIB_ROOT} environment variable, then \code{option(datalib.root)}, then
the \verb{datalib:} key in the user block of \verb{~/.config/user_config.yml}.}
}
\value{
A data.frame with one row per adaptation vintage folder and
columns:
\describe{
\item{collection}{character; adaptation collection code (e.g. \code{"HLT"}).}
\item{master_version}{integer; master vintage the adaptation builds on.}
\item{adaptation_version}{integer; adaptation vintage number.}
\item{folder}{character; on-disk vintage folder name.}
}
Zero rows when the survey has no adaptations.
}
\description{
Lists every adaptation vintage folder of one survey: the rows of
\code{\link[=datalib_vintages]{datalib_vintages()}} with \code{kind == "adaptation"}. Mirrors Stata
\verb{_adaptcheck} with a zero-row data.frame instead of the \code{"0"} sentinel.
Inputs are trimmed and uppercased once at the boundary and matched
exactly against folder names (contract v1, \code{config/grammar.md}).
}
\examples{
\dontrun{
datalib_adaptations("MWI", 2019, "MICS6")
datalib_adaptations("mwi", "2019", "mics6", root = "C:/data/library")
}
}
