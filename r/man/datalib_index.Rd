% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_index.R
\name{datalib_index}
\alias{datalib_index}
\title{Walk a subtree recursively into a data.frame}
\usage{
datalib_index(
  path = NULL,
  root = NULL,
  max_nodes = 400L,
  max_depth = 0L,
  dirs = FALSE,
  sizes = FALSE,
  pattern = NULL,
  verbose = FALSE
)
}
\arguments{
\item{path}{Subtree to start from, relative to \code{root}, used exactly as
given. \code{NULL} walks from the root -- read the cost section first.}

\item{root}{Tree to walk; defaults to the configured datalib root. No
library test is applied.}

\item{max_nodes}{Stop after this many folders. Default 400.}

\item{max_depth}{Stop descending past this depth; the children of the
starting node are depth 1. \code{0} (default) means no limit.}

\item{dirs}{Also emit a row per folder. Folders are traversed and counted
either way.}

\item{sizes}{Measure each file. Off by default; see the cost section.}

\item{pattern}{Optional regular expression; only files whose \strong{name}
matches are indexed. Folders are always traversed regardless, or the walk
could not reach a nested match.}

\item{verbose}{Report progress every 25 folders.}
}
\value{
A data.frame with columns \code{relpath}, \code{parent}, \code{name}, \code{ext},
\code{depth}, \code{is_dir}, \code{bytes}, \code{looks_grammar}, and attributes \code{nodes},
\code{n_files}, \code{n_dirs}, \code{truncated}, \code{seconds}, \code{root}, \code{path}.

There are deliberately \strong{no} country or survey columns. The reason this
function and \code{\link[=datalib_explorer]{datalib_explorer()}} exist is that these trees do \emph{not}
follow the naming grammar, so a built-in "component 1 is a country" would
smuggle back the assumption they were written to avoid. Where a tree does
follow it, splitting \code{relpath} on \code{/} is one line and yours to name.
}
\description{
Returns one row per file -- and with \code{dirs = TRUE} one per folder -- for a
whole branch of any folder tree, whether or not it follows the datalib
naming grammar. Where \code{\link[=datalib_explorer]{datalib_explorer()}} answers "what is in \emph{this}
folder", this answers "what is in this whole branch", which is the form you
can \code{table()}, \code{merge()} or \code{write.csv()}.
}
\section{The cost is real, and it is not this code}{

Measured on the network share this package targets, a recursive walk costs
about \strong{0.35 seconds per folder}, near-constant across subtrees of very
different size (a large branch in about nine minutes; a mid-sized branch faster still; a small branch faster again). That is
SMB round-trip latency on each directory open, and a single bulk
enumeration is no faster -- PowerShell's \code{Get-ChildItem -Recurse} took
538 s on the same subtree and found the same folders and files. There is no
fast path from one thread.

So \code{max_nodes} defaults to 400 rather than infinity, and hitting it is
reported rather than silent: the \code{truncated} attribute is \code{TRUE} and the
result is a \emph{prefix} of the subtree. For whole-archive work use the
scheduled catalogue, which pays the same per-folder cost in parallel and
off-hours and stores checksums this function deliberately does not compute.
}

\section{Difference from the Stata implementation}{

None that matters. Stata's \code{datalib_index} returns a dataset and so does
this; the columns and their meanings are identical, which is what makes the
shared conformance cases possible. Stata prints progress because a
nine-minute walk needs feedback in an interactive console; here that is
\code{verbose}.
}

\examples{
\dontrun{
idx <- datalib_index("Afghanistan", root = "<staging-tree>")
table(idx$ext)
attr(idx, "truncated")
}
}
\seealso{
\code{\link[=datalib_explorer]{datalib_explorer()}} for a single node.
}
