% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datalib_map_drive.R
\name{datalib_map_drive}
\alias{datalib_map_drive}
\title{Map a Windows network drive}
\usage{
datalib_map_drive(
  letter = NULL,
  unc = NULL,
  user = NULL,
  config = NULL,
  persistent = TRUE,
  force = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{letter}{Drive letter to map, accepted as \code{"Z"}, \code{"z:"}, or
\code{"Z:/"} (normalized to \code{"Z:"}). When \code{NULL}, the user config \code{zDrive}
(default \code{"Z:/"}).}

\item{unc}{UNC path to map (e.g. \code{"\\\\\\\\server\\\\share"}). When \code{NULL},
the user config \code{zDriveUNC}.}

\item{user}{User name whose configuration block supplies the defaults;
passed to \code{\link[=datalib_config]{datalib_config()}}. When \code{NULL}, the logged-in user. Only
consulted when \code{letter} or \code{unc} is missing.}

\item{config}{Path to the configuration YAML file; passed to
\code{\link[=datalib_config]{datalib_config()}}. When \code{NULL}, \verb{~/.config/user_config.yml}. Only
consulted when \code{letter} or \code{unc} is missing.}

\item{persistent}{Logical; when \code{TRUE} (default) the mapping is made
with \verb{/persistent:yes}, otherwise \verb{/persistent:no}.}

\item{force}{Logical; when \code{TRUE}, an existing mapping of \code{letter} to a
DIFFERENT remote is deleted and remapped. When \code{FALSE} (default), the
call returns status \code{"failed"} instead.}

\item{dry_run}{Logical; when \code{TRUE}, nothing is executed and status
\code{"dryrun"} is returned with the normalized \code{letter} and \code{unc}.}
}
\value{
A named list:
\describe{
\item{status}{character; one of \code{"mapped"}, \code{"already-mapped"},
\code{"dryrun"}, or \code{"failed"}.}
\item{letter}{character; normalized drive letter (e.g. \code{"Z:"}).}
\item{unc}{character; the UNC path involved (for
\code{"already-mapped"}/blocked \code{"failed"}, the currently mapped
remote).}
}
}
\description{
Windows \verb{net use} wrapper mirroring the Stata \code{mapzdrive} alias (plus
private \verb{_mzremote}). Maps \code{letter} to \code{unc}, defaulting both from the
user configuration block (\code{zDrive} / \code{zDriveUNC}, see
\code{\link[=datalib_config]{datalib_config()}}). Mapping FAILURE is a status, not an error (parity
with Stata); calling on a non-Windows platform raises a condition of
class \code{datalib_error_input} (Stata 198), as does an invalid \code{letter} or
a missing UNC path.
}
\examples{
\dontrun{
datalib_map_drive()                                   # config defaults
datalib_map_drive("Z", "\\\\\\\\server\\\\share")
datalib_map_drive("Z", "\\\\\\\\server\\\\share", dry_run = TRUE)
datalib_map_drive("Z", "\\\\\\\\server\\\\other", force = TRUE)
}
}
